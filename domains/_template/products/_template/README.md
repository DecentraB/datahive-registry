# Data Product Template

Copy this directory to `domains/<domain>/products/<product>`.

Expected ownership:

- `product.yaml`: ODPS-facing product metadata entrypoint.
- `contracts/`: ODCS output-port contracts.
- `runtime/`: product-owned runtime configuration references.
- `envs/`: deploy values for each environment.

