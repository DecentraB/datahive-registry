# Domain Template

Copy this directory to `domains/<domain>` when onboarding a new domain.

Domain teams own the product folders below `products/`. Platform teams should only add shared guardrails and validation, not product-specific runtime logic.

