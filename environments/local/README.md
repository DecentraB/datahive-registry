# Local Environment

Local is for developer bootstrap and fast validation.

Platform-owned foundation infrastructure lives in `foundation/terragrunt`. Tenant-owned product configuration stays under `domains/<domain>/products/<product>/envs/local`.

